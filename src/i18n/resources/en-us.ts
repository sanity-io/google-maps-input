/* eslint sort-keys: "error" */
export default {
  /** When field _has_ a value, text shown on the button to open an edit dialog */
  'action.edit': 'Edit',

  /** When field _has_ a value, text shown on button to remove (unset) value  */
  'action.remove': 'Remove',

  /** When field _does not_ have a value, text shown on the button to open an edit dialog */
  'action.set-location': 'Set location',

  /** Text prompting user to place the marker on the map */
  'edit-dialog.place-marker': 'Place the marker on the map',

  /** When field has a value, we show a preview image to avoid having to load the entire Google Maps API unless editing. This is the alt text for that image. */
  'preview-image.alt-text-label': 'Map location',
}
